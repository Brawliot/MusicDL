# Clave privada RSA — NUNCA en el repo (vive en %USERPROFILE%\.secrets\MusicDL\)
clave-privada.xml
clave-privada.pem
*.pem
*.key

# Empaque generado (usar Build-Zip.ps1)
MusicDL.zip

# Parches / dumps locales de edición
_patch_dump/

# Datos locales de ejecución / caché (si los hubiera en la carpeta del repo)
.prev-version.bat
*.tmp
dl-*.tmp
__pycache__/
.DS_Store
Thumbs.db
desktop.ini

# Editores / entorno
.vscode/
.idea/
*.swp
*.sig.bak*
*.bak-ux
*.sig.bak-ux
